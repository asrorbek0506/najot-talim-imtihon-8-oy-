export interface Milestone {
  year: string;
  title: string;
  desc: string;
}

export interface TeamMember {
  photo: string;
  name: string;
  role: string;
}

export interface ValueItem {
  icon: string;
  title: string;
  desc: string;
}
